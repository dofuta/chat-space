=link_to group_messages_path(group), class:"ChatSide__group" do
  .ChatSide__groupName
    =group.name
  .ChatSide__groupFirstMessage
    =group.messages[-1].present? ? group.messages[-1].text : "新着メッセージはありません"
